##
## a template to produce a graphviz diagram for the EFSM using aliases
## to label states and transitions
##
/*
 * Automatically generated by $tool.name $tool.version
 * template engine: velocity
 * template file: efsm2ct/plugin/dot-fittest.vm
 * ver 1.2
 */

digraph FSM  {

	node [style=rounded]
	

    #foreach ($s in ${model.states})
    	#if(${s.predicate} == "false")
        ${s.name} [shape="record", label="${s.name} : init"];
        #else
         ${s.name} [shape="record", label="${s.name} : ${s.dottyPredicate}"];
        #end
    #end
    

    #foreach ($s in ${model.states})
        #foreach ($t in ${s.outgoing})
            ${t.source.name} -> ${t.target.name} [ arrowhead = "vee", label="${t.mutator.alias} : ${t.mutator.name}"];
        #end
    #end
}




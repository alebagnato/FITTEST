##
## a template to produce a graphviz diagram for the EFSM using aliases
## to label states and transitions
##
/**
 * Automatically generated by $tool.name $tool.version
 * template engine: velocity
 * template file: dot-alias.vm
 */

digraph "${model.sutName}"  {

    #foreach ($s in ${model.states})
        ${s.name} [label="${s.name}"];
    #end
    

    #foreach ($s in ${model.states})
        #foreach ($t in ${s.outgoing})
            ${t.source.name} -> ${t.target.name} [ label="${t.mutator.alias}"];
        #end
    #end
}
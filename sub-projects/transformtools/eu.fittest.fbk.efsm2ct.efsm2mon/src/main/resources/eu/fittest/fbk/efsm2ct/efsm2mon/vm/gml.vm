#
# * Automatically generated by $tool.name $tool.version
# * template engine: velocity
# * template file: gml.vm
# *

graph
[
	hierarchic	1
	directed	1
        label "${model.sutName}"

        #foreach ($s in ${model.states})
        
        node
	[
		id	${s.id}
		label	"${s.name}"
	]
        #end

	
	#foreach ($s in ${model.states})
        #foreach ($t in ${s.outgoing})
           
        edge
	[
		source	${t.source.id}
		target	${t.target.id}
		label	"${t.mutator.name}"
	]

        #end
        #end
	
        
]

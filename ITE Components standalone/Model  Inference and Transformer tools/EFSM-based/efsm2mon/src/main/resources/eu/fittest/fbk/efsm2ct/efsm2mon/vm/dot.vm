##
## a template to produce a graphviz diagram for the EFSM
##
/**
 * Automatically generated by $tool.name $tool.version
 * template engine: velocity
 * template file: dot.vm
 */

digraph "${model.sutName}"  {

    #foreach ($s in ${model.states})
        ${s.name} [label="${s.name}: ${s.predicate}"];
    #end
    

    #foreach ($s in ${model.states})
        #foreach ($t in ${s.outgoing})
            ${t.source.name} -> ${t.target.name} [ label="${t.mutator.name}"];
        #end
    #end
}